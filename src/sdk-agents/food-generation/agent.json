{
  "id": "food-generation",
  "version": "2.0.0",
  "type": "agent",
  "description": "Generates food items based on description and criteria",
  "interface": {
    "input": { "$ref": "food-generation:input" },
    "output": { "$ref": "food-generation:output" }
  },
  "mcpServers": {},
  "config": {
    "tier": "balanced",
    "temperature": 1,
    "maxSteps": 10,
    "maxTokens": 8192,
    "timeout": 120000,
    "executionMode": "durable",
    "skipSynthesis": true,
    "outputArtifact": "foods",
    "durability": {
      "enabled": true,
      "maxDurationMs": 600000,
      "retry": {
        "maxAttempts": 3,
        "backoffMs": 1000,
        "backoffMultiplier": 2
      },
      "checkpointStrategy": "step"
    }
  }
}
